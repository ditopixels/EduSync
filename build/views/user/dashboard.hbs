<div class="dash">
    {{>header}}
    <div class="dashContent space-between">
        {{>navigator}}
        <main class="rol {{contentRol.args.class}}">
            {{>(contentRol.partial) args=contentRol.args}}
        </main>
    </div>
</div>